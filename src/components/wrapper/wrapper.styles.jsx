import styled from '@emotion/styled'

export const Wrapper = styled.div(
  ({theme}) => ({
    margin: '0 auto',
    maxWidth: '960px',
    padding: theme.spacing.lg,
    backgroundColor: '#fff'
  })
)
